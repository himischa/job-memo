# Job Memo API

Spring Boot REST API for Job Memo.

## Setup
- Copy .env.example to .env and fill in values
- Start Postgres with `docker compose up -d` from `job-memo-api/`
- Run with mvn spring-boot:run
